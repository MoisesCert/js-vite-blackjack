import { defineConfig } from 'vite';

// base hace que al hacer el build en las rutas relativas le pong por defecto ese texto al principio
export default defineConfig({
    base: '/js-vite-blackjack/',
    plugins: []
});

